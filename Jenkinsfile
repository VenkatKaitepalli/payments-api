pipeline {
         agent any
         stages {
                 stage('Configs') {
                 steps {
                     echo 'Starting to build the App.'
                 }
                 }
                 stage('Check DB changes and Deploy') {
                 steps {
                     echo 'Starting to build the App.'
                 }
                 }                  
                 stage('Setup Ephemeral project') {
                 steps {
                    echo "Start the deploy .."
                 } 
                 }                 
                 stage('Unit Test') {
                 steps {
                     echo 'Starting to build the App.'
                 }
                 }
                 stage('Code Analysis') {
                 steps {
                    echo('Starting Junit the App.')
                 }
                 }
                 stage('Build') {
                 steps {
                    echo('Starting Junit the App.')
                 }
                 }
                 stage('Deploy to Ephemeral project') {
                 steps {
                    echo "Start the deploy .."
                 } 
                 }
            }
}
